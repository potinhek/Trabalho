name: CD - Deploy to Kubernetes (Self-Hosted)

on:
  workflow_run:
    workflows: ["CI Build & Push Docker"]  # mesmo nome do CI
    types: ["completed"]

permissions:
  contents: read

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: [self-hosted, Windows]  # ajuste se o label do runner for diferente
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set vars
        id: vars
        shell: powershell
        run: |
          "IMAGE=jacksonugv/trabalho" >> $env:GITHUB_OUTPUT

          # SHA do workflow de CI que terminou
          $sha = "${{ github.event.workflow_run.head_sha }}"
          "TAG_SHA=$($sha.Substring(0,7))" >> $env:GITHUB_OUTPUT

          $ns = "${{ secrets.K8S_NAMESPACE }}"
          if ([string]::IsNullOrWhiteSpace($ns)) { $ns = "default" }
          "NAMESPACE=$ns" >> $env:GITHUB_OUTPUT

      # --- kubectl disponível ---
      - name: Ensure kubectl
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          if (-not (Get-Command kubectl -ErrorAction SilentlyContinue)) {
            Write-Host "kubectl não encontrado. Instalando..."
            Invoke-WebRequest -UseBasicParsing -Uri https://dl.k8s.io/release/stable.txt -OutFile stable.txt
            $ver = Get-Content stable.txt
            $url = "https://dl.k8s.io/release/$ver/bin/windows/amd64/kubectl.exe"
            $bin = Join-Path $env:RUNNER_TEMP "bin"
            New-Item -ItemType Directory -Force -Path $bin | Out-Null
            Invoke-WebRequest -UseBasicParsing -Uri $url -OutFile (Join-Path $bin "kubectl.exe")
            "$bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding ascii -Append
          }
          kubectl version --client=true

      # --- Minikube ligado e com contexto ativo ---
      - name: Ensure Minikube running
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'

          if (-not (Get-Command minikube -ErrorAction SilentlyContinue)) {
            throw "Minikube não está instalado no runner. Instale no mesmo host."
          }

          # Driver preferível
          $driver = "docker"
          if (-not (Get-Command docker -ErrorAction SilentlyContinue)) {
            $driver = "hyperv"
          }

          # Verifica se já está rodando
          $ok = $false
          try {
            $st = minikube status --output=json | ConvertFrom-Json
            if ($st.Host -eq 'Running' -and $st.Kubelet -eq 'Running' -and $st.APIServer -eq 'Running') { $ok = $true }
          } catch { }

          if (-not $ok) {
            Write-Host "Iniciando Minikube (--driver=$driver)..."
            minikube start --driver=$driver
          }

          # Garante kubeconfig do minikube e contexto
          minikube update-context

          $ctx = kubectl config current-context
          Write-Host "Contexto atual: $ctx"
          $server = kubectl config view --minify -o jsonpath='{.clusters[0].cluster.server}'
          Write-Host "API Server: $server"

      # --- Health-check do API server com retry ---
      - name: Wait for API server (healthz)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $max = 20
          for ($i=1; $i -le $max; $i++) {
            try {
              $h = kubectl get --raw=/healthz 2>$null
              if ($LASTEXITCODE -eq 0 -and $h -match "ok") {
                Write-Host "API Server OK"; break
              }
            } catch { }
            Write-Host "Aguardando API Server ($i/$max)..."
            Start-Sleep -Seconds 3
            if ($i -eq $max) { throw "API Server indisponível (timeout)." }
          }
          kubectl cluster-info
          kubectl get nodes -o wide

      # --- Namespace (idempotente) ---
      - name: Create namespace if not exists
        shell: powershell
        env:
          NS: ${{ steps.vars.outputs.NAMESPACE }}
        run: |
          kubectl get ns $env:NS *> $null
          if ($LASTEXITCODE -ne 0) { kubectl create ns $env:NS }
          kubectl get ns $env:NS

      # --- Manifests base ---
      - name: Apply base manifests
        shell: powershell
        env:
          NS: ${{ steps.vars.outputs.NAMESPACE }}
        run: |
          $ErrorActionPreference = 'Continue'

          $files = @(
            "k8s/service.yaml",
            "k8s/prometheus-deployment.yaml",
            "k8s/grafana-deployment.yaml",
            "k8s/nginx-deployment.yaml",
            "k8s/nginx-service.yaml",
            "k8s/prometheus-config.yaml"
          )

          foreach ($f in $files) {
            if (Test-Path $f) {
              Write-Host "Aplicando $f..."
              kubectl apply -n $env:NS -f $f
              if ($LASTEXITCODE -ne 0) {
                Write-Host "⚠ Falha ao aplicar $f, continuando..."
              }
            } else {
              Write-Host "⚠ Arquivo não encontrado: $f (ignorando)."
            }
          }

      # --- App principal ---
      - name: Apply app deployment
        shell: powershell
        env:
          NS: ${{ steps.vars.outputs.NAMESPACE }}
        run: |
          kubectl apply -n $env:NS -f k8s/deployment.yaml

      - name: Set image to new tag
        shell: powershell
        env:
          NS:  ${{ steps.vars.outputs.NAMESPACE }}
          IMG: ${{ steps.vars.outputs.IMAGE }}
          TAG: ${{ steps.vars.outputs.TAG_SHA }}
        run: |
          kubectl set image deployment/trabalho trabalho="$env:IMG:$env:TAG" -n $env:NS

      - name: Wait rollout
        shell: powershell
        env:
          NS: ${{ steps.vars.outputs.NAMESPACE }}
        run: |
          kubectl rollout status deployment/trabalho -n $env:NS --timeout=180s

      - name: Show services/pods
        shell: powershell
        env:
          NS: ${{ steps.vars.outputs.NAMESPACE }}
        run: |
          kubectl get svc -n $env:NS
          kubectl get deploy -n $env:NS
          kubectl get pods -o wide -n $env:NS
